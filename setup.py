from setuptools import setup, find_packages

setup(
    name='gmpScan',
    description='A simple Python library for create scan',
    author='Magistrello Camilla',
    packages=find_packages(),
)